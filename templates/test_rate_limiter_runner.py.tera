"""
run_sim.py — cocotb 2.0 programmatic runner for rate_limiter tests
Generated by pacgate — do not edit
"""

import os
from cocotb_tools.runner import get_runner


def main():
    sim = os.environ.get("SIM", "icarus")
    runner = get_runner(sim)

    runner.build(
        verilog_sources=["../../rtl/rate_limiter.v"],
        hdl_toplevel="rate_limiter",
        always=True,
    )

    runner.test(
        hdl_toplevel="rate_limiter",
        test_module="test_rate_limiter",
        results_xml="results.xml",
    )


if __name__ == "__main__":
    main()
